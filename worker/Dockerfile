FROM node:22-bookworm@sha256:5647be709086c696ff32edaaf1c70cd26d1da6ab2b39c32f3c7b4c4a31957e37

# Contract 1.6 supplies the numeric unprivileged user, read-only root, limits,
# copied payload, fixed command, and attempt-local scratch at container create.
# Keeping this image layer-free makes the reviewed runtime reference identical
# to the pinned upstream digest below.
