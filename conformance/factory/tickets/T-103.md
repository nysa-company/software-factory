# T-103 — rejections carry a reason

State: In progress
Branch: ticket/T-103-reject-reason
Risk class: low (approval lifecycle metadata, no external actions)

## Description

When an operator rejects a proposed action today (`POST /api/approvals/:id/reject`), the proposal just closes. In a real approval flow the *why* matters: audits and the proposing side both need it. Let rejections carry an optional reason.

- `POST /api/approvals/:id/reject` accepts an optional JSON body `{ "reason": "<string>" }`. The reason is stored on the approval record and survives restarts (persisted with the rest of the state).
- The approval as returned by `GET /api/state` exposes the reason on rejected approvals. Absent a reason, the field is present and null (planner freezes the exact representation).
- Rejecting an approval that is not `pending` must not change it and must be refused with a clear status code (planner freezes which).

## Acceptance criteria (planner freezes the exact contract)

1. Reject with a reason → the approval in `/api/state` is `rejected` and carries that exact reason string. → satisfied when the approval object matches Frozen contract §"Rejected with reason" exactly.
2. Reject without a body or without a reason → the approval is `rejected` with the frozen "no reason" representation. → satisfied when the approval object matches Frozen contract §"Rejected without reason" exactly.
3. Rejecting an already-rejected (or otherwise non-pending) approval → refused with the frozen status code (`409`) and the record is unchanged, including its original reason. → satisfied when the response matches Frozen contract §"Reject on non-pending" and a follow-up `/api/state` fetch shows the approval byte-for-byte unchanged from before the second reject call.
4. Reason survives a server kill and restart (persisted atomically like all other state). → satisfied when, after `SIGKILL` + restart, the approval object in `/api/state` still matches Frozen contract §"Rejected with reason" exactly (same pattern as `conformance.test.js` test 7).

## Frozen contract

### `POST /api/approvals/:id/reject`

Request body: optional JSON `{ "reason": "<string>" }` (`Content-Type: application/json`; a missing/empty body is legal, matching today's behavior).

- `reason` resolution: if the parsed body has a `reason` key whose value is a `string` (including `""`), that exact string is stored. In every other case — no body, no `reason` key, `reason: null`, or a non-string `reason` (number/object/array/bool) — the stored reason is `null`. No 400 is raised for a non-string `reason`; it is silently treated as absent. (Out of scope: this ticket does not add body-shape validation beyond this rule.)
- Unchanged from current behavior: unknown `:id` → `404 { "ok": false, "error": "no such approval" }`. Non-`pending` approval → `409 { "ok": false, "error": "already <status>" }`, and the approval record is not modified in any way (status, reason, or otherwise).
- On success (approval was `pending`): approval transitions to `rejected`, its `reason` is set per the resolution rule above, state is persisted. Response is `200`:
  ```json
  { "ok": true, "status": "rejected", "reason": "<string-or-null>" }
  ```

### `GET /api/state` — `approvals[]` shape

- Approval objects for status `pending`, `sent`, or `blocked_recipient` are **unchanged**: no `reason` key is added to them.
- An approval object gains a `reason` key **only when/if it is rejected**, at the moment of rejection, and never removed or changed afterward (a second reject call is refused per above, so once set it is immutable).
  - **Rejected with reason** — reject called with `{ "reason": "no longer needed" }`:
    ```json
    { "id": "appr-e1", "jobId": "job-e1", "action": { "...": "..." }, "status": "rejected", "proposedAt": "...", "reason": "no longer needed" }
    ```
  - **Rejected without reason** — reject called with no body, `{}`, or a non-string `reason`:
    ```json
    { "id": "appr-e1", "jobId": "job-e1", "action": { "...": "..." }, "status": "rejected", "proposedAt": "...", "reason": null }
    ```
  - **Reject on non-pending** — second reject call on either of the above returns `409 { "ok": false, "error": "already rejected" }`; the approval object retrieved from a subsequent `/api/state` call is identical (including `reason`) to what it was right after the first reject.

If this contract proves wrong during build or test-authoring, the ticket goes back to Ready for re-planning as a new version — do not silently change the shape.

## Existing test audit (planner note lesson from T-102)

Every test file in `conformance/app/tests/` was read in full and checked for exact-body assertions this change could touch:

- `tests/health.test.js` (4 `assert.deepStrictEqual` calls on `GET /health` bodies) and `tests/health-approvals.test.js` (3 exact `JSON.stringify` matches on `GET /health` bodies) — both assert only the aggregate `{ ok, queue, approvals: {pending,sent,rejected,blocked_recipient} }` shape from the `/health` summary endpoint. This ticket does not touch `/health` or its counts; the per-approval `reason` field lives on `/api/state` records, which `/health` never returns verbatim. **Not affected, no edits needed.**
- `tests/conformance.test.js` test `"6b. reject: closes the proposal, nothing sends"` asserts `r.status === "rejected"` (a single-property `assert.strictEqual`, not a full-object/deepStrictEqual match) plus outbox length and the post-reject `approve` 409. Adding a `reason` key to the reject response body and to the approval record does not change `.status` and is not caught by a property-level assertion. **Not affected, no edits needed.**
- No test file anywhere does `assert.deepStrictEqual` (or an exact `JSON.stringify`/full-body match) against an individual approval object from `/api/state` or against the full `POST /reject` response body.

**Conclusion: zero existing tests require amendment.** This is an audited result, not an assumption — the test-author does not need to touch any pre-existing test file for this ticket; `tests/reject-reason.test.js` is a purely additive new file.

## Conventions

- App lives in `conformance/app/` (zero-dependency Node 18+; see `conformance/SPEC.md`).
- Tests go in `conformance/app/tests/` — new file `tests/reject-reason.test.js`, same style as the existing suites (node:test, spawn the real server on its own port with a temp DATA_DIR).
- Run tests from `conformance/app/` with `node --test tests/reject-reason.test.js`.

## Labels

No `external` label — rejecting a proposal cannot trigger a send (outbox is untouched by this ticket); this only adds metadata to the approval record.

## Log

- 2026-07-12: ticket created by the operator; State → Ready. First ticket of the loop trial (dispatcher runs stage-to-stage without per-stage operator go).
- 2026-07-12: planner froze the `POST /api/approvals/:id/reject` reason contract and the `reason` field on rejected `/api/state` approvals; audited all three existing test files and found zero exact-body collisions (see "Existing test audit") — `tests/reject-reason.test.js` is purely additive, no pre-existing test needs amendment; branch `ticket/T-103-reject-reason` created off `main`; State → In progress.
- 2026-07-12: planner (Claude Code) completed the frozen contract and branch/worktree setup; ledger cost $1.0881249. State → Blocked-Escalated because the wrapper reported: "WARNING: installed Claude Code (2.1.201 (Claude Code)) != pinned (2.1.207). Run adapters/contract-test.sh before continuing." Javier must run that contract test or reconcile the installed/pinned version, then explicitly authorize resumption.
- 2026-07-12: operator resolved the adapter escalation: `scripts/adapters/contract-test.sh` passed against Claude Code 2.1.201 and Codex 0.144.1, and `CLAUDE_CODE_PINNED=2.1.201` is now set in `~/.factory/global.env`; State → In progress and stage-to-stage supervision resumed.
- 2026-07-12: test author added `tests/reject-reason.test.js` with one acceptance test each for: exact reason storage and response; null resolution for missing/non-string reasons; immutable rejection with exact `409`; and reason persistence across `SIGKILL` + restart. All four criteria were expressible as tests; no demo-check-only items.
- 2026-07-12: test-author (Codex) committed four acceptance tests at `672a598`; new suite fails 4/4 for the expected missing behavior while all 15 existing tests pass; no implementation code changed; ledger cost $0.4749.
- 2026-07-12: test-author verification expanded criterion 3 to cover every reachable non-pending status (`rejected`, `sent`, and `blocked_recipient`), asserting the exact `409` body and byte-for-byte unchanged approval record; the four acceptance tests still fail 4/4 for missing contract behavior, while all 15 existing tests pass. All criteria remain test-expressible; no demo-check items.
- 2026-07-12: test-author (Codex) completed sequencer-selected verification at `f371048`; coverage now includes all reachable non-pending statuses, acceptance tests fail 4/4 as intended, and existing tests pass 15/15; ledger cost $0.4943.
- 2026-07-12: State → Blocked-Escalated before the builder started because the wrapper refused exactly: "daily cap would be exceeded (spent $8.9678 + reserve $1.50 > $10.00) — refusing. See runbooks/operator.md." Javier must resolve or reset the daily budget through the operator runbook and explicitly authorize resumption; no builder run or cost was recorded.
- 2026-07-12: operator confirmed the daily-cap refusal was correct and raised `DAILY_CAP_USD` to $13.00 on main while retaining the machine-level $15.00 cap; State → In progress and stage-to-stage supervision resumed.
- 2026-07-12: builder (Claude Code) implemented the frozen reject-reason contract in `conformance/app/server.js` without touching tests; all 19 tests pass (15 existing + 4 acceptance), commit `f3d85bb`; ledger cost $0.5681364000000001.
- 2026-07-12 22:36:03: reviewer round 1: REQUEST CHANGES — The frozen unknown-ID contract is untested. The builder changed reject-response serialization to remove the internal `code` field, but tests only verify exact `409` bodies. An implementation that fixes `409` while returning the wrong `404` body would pass all 19 tests. Add a test asserting an unknown approval returns HTTP `404` with exactly `{ "ok": false, "error": "no such approval" }`. Criteria 1–4 are otherwise adequately tested. Commit order and test immutability are correct, and the full suite independently passes 19/19. Cost $0.2206.
- 2026-07-12: test-author (Codex) addressed reviewer round 1 by adding exact unknown-approval HTTP 404/body coverage at `257619e`; targeted suite passes 5/5 and full suite passes 20/20; only `tests/reject-reason.test.js` changed; ledger cost $0.2461.
